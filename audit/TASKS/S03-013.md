# S03-013 — Fix issue in api_audit.md

## Status
TODO

## Priority
P2

## Sprint
Sprint 3 — Data & API Reliability

## Type
IMPLEMENTATION

## Objective
Selesaikan temuan terkait Fix issue in api_audit.md.

## Background
Validasi minim/tidak konsisten: `int(data.get("volume", 80))`, `int(data.get("index", 0))`, `float(data.get("position", 0))` dipanggil langsung tanpa cek batas (mis. `volume` bisa negatif atau > `MAX_VOLUME=150`, `index` bisa negatif/di luar panjang queue)

## Source Report
api_audit.md

## Source Finding
API-22

## Affected Files
- Diidentifikasi dalam laporan

## Dependencies
Depends On: None

## Implementation Notes
Asumsi priority berdasarkan rule normalisasi.

## Acceptance Criteria
- Validasi range eksplisit sebelum `command_bus.execute` (clamp volume 0–150, index 0..len(queue)-1, dst.), jangan andalkan lapisan command/engine untuk menahan semua input tak terduga

## Definition of Done
Kode berhasil diubah, test lewat, masalah di laporan terselesaikan.

## Validation
Lakukan manual review dan run test.

## Checklist
- [ ] Implementasi
- [ ] Build berhasil
- [ ] Test berhasil
- [ ] Regression check
- [ ] Documentation updated
