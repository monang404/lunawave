# Task ID: S05-002

**Title:** config.py menjalankan side effects
**Priority:** P3
**Sprint:** sprint-05-maintainability
**Status:** DONE

## Source Audit
[EXEC-003, ARCH-A09]

## Root Cause
File config.py menjalankan side effects saat proses import (seperti membuat direktori socket dan memvalidasi path), yang mana melanggar prinsip dasar modul Python. File konfigurasi langsung mengeksekusi operasi sistem operasi (mkdir socket dan file I/O). Pemanggilan fungsi ber-side-effect pada modul level top melanggar Clean Architecture dan akan memicu error di saat sistem import daripada saat runtime.

## Location
`config.py:12-14`

## Current Behavior
Eksekusi _side-effect_ dengan membuat folder (operasi IO) dieksekusi secara top-level persis di saat proses _import_ module `config`.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Pindahkan deklarasi modul `import` ke bagian paling atas file sesuai PEP 8.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-217', 'M-142', 'M-218']

## Conflicts With
['M-090', 'M-150']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait
