# Task ID: S01-017

**Title:** /metrics Menggunakan Custom Header X-Metrics-Token (Non-Standard)
**Priority:** P1
**Sprint:** sprint-01-security
**Status:** DONE

## Source Audit
[API-16]

## Root Cause
Rute layanan prometheus mendadak menggunakan X-Metrics-Token custom pada pengamanan rahasianya, yang secara inheren ditolak berbagai engine analitik lain yang biasanya menggunakan skema Bearer Auth konvensional.

## Location
`server/handlers/http.py:199`

## Current Behavior
Jendela eksportir telemetri metrik Prometheus malah disekat dengan _header authentication custom_ bernama `X-Metrics-Token`, membelot dari pakem de facto standar industri Prometheus server yang mutlak mengharap format sisipan kata sandi standar (skema `Authorization: Bearer <token>`).

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Konfigurasikan security headers HTTP dengan ketat. Hilangkan wildcard '*' dari konfigurasi CORS Allow-Origin dan batasi domain asal.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-036']

## Conflicts With
['M-007', 'M-013', 'M-077', 'M-117']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S01-017
- **Objective**: Mengganti otorisasi metrik menjadi format standar `Authorization: Bearer <token>`.
- **Summary**: Menulis ulang implementasi validasi token rahasia pada rute `serve_metrics()` di `server/handlers/http.py`. Skema custom `X-Metrics-Token` diubah menjadi skema standar industri yaitu mem-parsing _header_ `Authorization: Bearer <token>`. Hal ini membuat rute metrik menjadi kompatibel langsung dengan _scrapers_ Prometheus dan platform analitik lainnya.
- **Files Modified**: 
  - `server/handlers/http.py`
  - `tests/unit/server/test_metrics_auth.py` (New Test)
- **Validation Result**: Lulus validasi secara unit test, di mana *request* tanpa header dan header yang tidak _match_ mendapatkan status 403 HTTP Forbidden dengan teks respons edukatif mengenai `Authorization: Bearer token`.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
