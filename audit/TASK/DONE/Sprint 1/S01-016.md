# Task ID: S01-016

**Title:** Admin Password Tidak Tercetak di Non-TTY Environment
**Priority:** P1
**Sprint:** sprint-01-security
**Status:** DONE

## Source Audit
[BUG-04, DEVOPS-015]

## Root Cause
Password login murni tak ter-hash yang di-generate perdana tercetak ke wadah shell console log sys.stderr, di mana oleh script eksekutor termux (android) dipaksa dilempar terekam utuh ke dalam log file plaintext (>> startup.log 2>&1). Menghamparkan pundi password bagi sesiapa yang nimbrung. Pencetakan password rahasia admin untuk sesi awal dikunci validasi output isatty() yang selalu salah (false) di lingkungan background (docker, daemon systemd), menyembunyikan selamanya password masuk yang ter...

## Location
`config.py:76-79`

## Current Behavior
Cetakan instruksi kata sandi pertama dilindungi proteksi `.isatty()`. Karena daemon di-docker dijalankan via _background detached_, nilai `.isatty()` akan me-_return_ `False`, sehingga log password akan hilang selamanya karena blok IF dilewati.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Identifikasi alur spesifik dari bug ini dan pastikan ada perbaikan yang memitigasi dampak dari masalah yang disebutkan di current behavior. Pastikan solusi aman dari sisi arsitektur.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-217']

## Conflicts With
['M-218']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S01-016
- **Objective**: Mencegah *plaintext password* admin bocor ke log sekaligus memastikan admin tetap bisa mengakses *password* di *non-TTY environment* (seperti Docker).
- **Summary**: Menghapus blok pengecekan `sys.stderr.isatty()` pada `config.py` yang selama ini mencegah pencetakan. Sebagai solusinya, kode generator kini akan menyalin *plaintext password* ke dalam sebuah *file* (sementara) bernama `cache/admin_initial_password.txt` dengan perizinan direktori ketat, dan `sys.stderr` kini HANYA akan mencetak informasi perihal keberadaan file tersebut tanpa memaparkan isi sandi aslinya ke `startup.log`.
- **Files Modified**: 
  - `config.py`
  - `tests/unit/test_config_password.py` (New Test)
- **Validation Result**: Uji Python memastikan bahwa berkas temporer berhasil tertulis beserta pesannya dan lulus pemeriksaan perizinan *filesystem*.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
