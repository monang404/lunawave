# Task ID: S02-011

**Title:** _on_track_ended error path: guard if IDLE tidak pernah terpenuhi
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B05]

## Root Cause
Pada kondisi error, state diatur ke PlayerStatus.ERROR sebelum sleep 2 detik. Guard 'if self.state.status == PlayerStatus.IDLE:' tidak akan pernah terpenuhi karena status saat itu adalah ERROR, mengakibatkan perpindahan track tak terhindarkan meski diputus manual oleh user.

## Location
`engine/playback/controller.py:186-192`

## Current Behavior
Kondisi guard IDLE tidak akan pernah terpenuhi karena nilai statusnya sudah ditetapkan keras ke ERROR sesaat sebelum jeda asinkron `sleep`.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Jangan menggunakan blok 'bare except' atau Exception generik. Tangkap spesifik Error yang diharapkan (misal ValueError/KeyError) dan log dengan formatter yang tepat menggunakan logger sistem, bukan sekadar diprint.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
['M-044', 'M-045', 'M-047', 'M-049', 'M-058', 'M-063', 'M-147']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-011
- **Objective**: Mengkoreksi logika _guard checking_ pasca-jeda error agar perpindahan antrean otomatis batal jika _state_ dimanipulasi manual.
- **Summary**: Pengecekan status pelindung asinkron di dalam rute kondisi `reason == "error"` pada `_on_track_ended` (file `engine/playback/controller.py`) direvisi dari `if self.state.status == PlayerStatus.IDLE:` menjadi `if self.state.status != PlayerStatus.ERROR:`. Pendekatan _negative check_ ini memastikan bahwa segala macam intervensi yang membelokkan kontrol player ke status selain `ERROR` (baik itu `IDLE` dari *stop*, maupun `LOADING` dari *skip next/prev* manual) selama *sleep delay* 2 detik tidak akan tertumpuk atau tersapu _auto-advance_.
- **Files Modified**: 
  - `engine/playback/controller.py`
  - `tests/unit/engine/test_playback_controller_error_guard.py` (New Test)
- **Validation Result**: Lulus. Tes *mock sleep delay* memverifikasi intervensi pengguna mengunci *auto-advance* agar tak dipanggil ganda/mem-bypass niat.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
