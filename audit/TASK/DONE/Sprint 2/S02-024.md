# Task ID: S02-024

**Title:** service_worker fallback salah path ke /static/index.html
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B19]

## Root Cause
Service Worker mencoba mencari dokumen HTML pada cache lewat caches.match('/static/index.html') padahal URL yang di-serve adalah path / (root). Offline fallback menjadi tidak berguna.

## Location
`web/static/sw.js:76-79`

## Current Behavior
`Service Worker` mencoba memuat _fallback offline_ `/static/index.html`, namun karena file rute index utamanya disajikan dari root `/`, file di path tersebut tidak akan ada dan PWA offline mode gagal beroperasi.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Identifikasi alur spesifik dari bug ini dan pastikan ada perbaikan yang memitigasi dampak dari masalah yang disebutkan di current behavior. Pastikan solusi aman dari sisi arsitektur.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-100']

## Conflicts With
Tidak ada

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-024
- **Objective**: Memperbaiki resolusi penautan berkas mundur (_fallback offline_) `Service Worker` agar kapabilitas _Progressive Web App_ (PWA) berjalan dengan mestinya saat jaringan terputus.
- **Summary**: Mengubah patokan *path* internal di `sw.js` dari yang sebelumnya memanggil `/static/index.html` (yang tidak pernah tersaji langsung) menjadi *root* murni (`/`). Modifikasi ini memulihkan ketersediaan mode luring (offline) karena `PRECACHE_ASSETS` memang mendaftarkan direktori `/` (BUG-B19).
- **Files Modified**: 
  - `web/static/sw.js`
  - `tests/unit/web/test_sw_fallback.py` (New Test)
- **Validation Result**: Lulus. Tes pemindaian teks memvalidasi bahwa blok `sw.js` memakai `/` dan menghindari `/static/index.html`.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
