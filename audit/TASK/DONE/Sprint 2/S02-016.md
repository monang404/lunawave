# Task ID: S02-016

**Title:** handle_ws_message kurang validasi tipe dict untuk 'data'
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B11]

## Root Cause
Handler memanggil data = msg.get("data", {}) namun apabila payload menspesifikasikan string untuk 'data', akan terjadi AttributeError di downstream ketika handler lanjutan mencoba untuk mengakses data.get("...").

## Location
`server/handlers/websocket.py:111-112`

## Current Behavior
Tidak ada jaminan validasi tipe objek dictionary dict pada `data`. Jika `data` dikirim client berwujud literal string, handler lanjutan yang mengeksekusi `get()` akan lumpuh.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Jangan menggunakan blok 'bare except' atau Exception generik. Tangkap spesifik Error yang diharapkan (misal ValueError/KeyError) dan log dengan formatter yang tepat menggunakan logger sistem, bukan sekadar diprint.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-010']

## Conflicts With
['M-033', 'M-052']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-016
- **Objective**: Melindungi *downstream handler* websoket dari *AttributeError* apabila klien mengirimi parameter `data` berformat tipe primitif seperti *string*.
- **Summary**: Implementasi tambahan validasi tipe pada _websocket handler_ utama. Jika objek dari `msg.get("data", {})` terdeteksi bukan _instance_ dari `dict`, fungsi memancarkan peringatan format _payload_ tak valid via `logger.warning` lalu menggeser nilai _data_ secara paksa (fallback) ke dictionary kosong `{}`. Pendekatan ini menyelamatkan fungsionalitas rutin `get()` yang diekspektasi semua subsistem kelanjutannya (seperti `handle_auth`, dll).
- **Files Modified**: 
  - `server/handlers/websocket.py`
  - `tests/unit/server/test_ws_data_type.py` (New Test)
- **Validation Result**: Lulus. Unit test memastikan _payload data_ berwujud `str` secara aman dikonversi dan tiba ke subsistem _auth_ sebagai dictionary kosong.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
