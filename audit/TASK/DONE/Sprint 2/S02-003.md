# Task ID: S02-003

**Title:** http_session tidak diinjeksikan
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[EXEC-005]

## Root Cause
aiohttp.ClientSession (http_session) dibuat di bootstrap.py namun tidak diinjeksikan ke server/app.py. Akibatnya, stream proxy di http.py akan diam-diam menginisialisasi None fallback.

## Location
`server/app.py:16-17`

## Current Behavior
`http_session` diinisialisasi di `core/bootstrap.py` tapi tidak pernah diloloskan menjadi argumen di fungsi `create_app` untuk _state_ aplikasi. Akibatnya ketika dipanggil dari `server/handlers/http.py`, `app.get("http_session")` bernilai None.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Identifikasi alur spesifik dari bug ini dan pastikan ada perbaikan yang memitigasi dampak dari masalah yang disebutkan di current behavior. Pastikan solusi aman dari sisi arsitektur.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
Tidak ada

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-003
- **Objective**: Memastikan `http_session` (instansi aiohttp.ClientSession) terinjeksi ke modul HTTP internal.
- **Summary**: Menambahkan parameter *keyword argument* opsional `http_session` ke *function definition* `create_app` di berkas `server/app.py`. Variabel injeksi tersebut lantas dimuat sebagai rujukan `app["http_session"]`. Baris _invoker_ `create_app` di `core/bootstrap.py` juga dikalibrasi sedemikian rupa untuk mengirim (pass) sesi koneksi HTTP, menghindari kembalian `None` secara implisit ketika _stream proxy handler_ `http.py` membutuhkannya.
- **Files Modified**: 
  - `server/app.py`
  - `core/bootstrap.py`
  - `tests/unit/server/test_app_http_session.py` (New Test)
- **Validation Result**: Lulus. Testing eksistensi variabel di blok _state_ internal membuktikan nilai objek injeksi diteruskan secara valid.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
