# Task ID: S02-023

**Title:** _on_track_ended dengan path eof terbuka pada pemanggilan paralel
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B18, EXC-04]

## Root Cause
Bug pada engine MPV bisa mengirimkan message end-file (eof) berulang kali berurutan. Tidak ada proteksi guard, yang menyebabkan fungsi _advance_to_next tereksekusi dua kali dan melompati dua trek antrean. Penanganan trigger waktu jeda EOF mpv sebesar (0.35s) tak diproteksi flag blokade boolean, membuat kiriman koneksi end-file redundan (misal 2 sinyal double karena lag internet) mengeksekusi track lompat 2x maju melompat secara brutal (skip song).

## Location
`engine/playback/controller.py:181-183`

## Current Behavior
Tidak terdapat mekanisme penguncian/flag boolean pelindung terhadap trigger paralel. Jika mesin mpv mengirim lebih dari satu notifikasi `eof` saat lag/masalah jaringan, maka trek bisa terlewati dua kali.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Identifikasi alur spesifik dari bug ini dan pastikan ada perbaikan yang memitigasi dampak dari masalah yang disebutkan di current behavior. Pastikan solusi aman dari sisi arsitektur.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
['M-044', 'M-045', 'M-046', 'M-047', 'M-049', 'M-063', 'M-147']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-023
- **Objective**: Melindungi eksekusi transisi otomatis (EOF) dari lonjakan _event_ redundan (misal akibat *network lag* pada engine MPV) yang menyebabkan pemutar melompat 2 lagu berurutan.
- **Summary**: Menambahkan _flag_ mutasi sekuensial `_eof_advancing` di `PlaybackController._on_track_ended` pada penanganan `eof`. Modifikasi ini mengunci fase `await asyncio.sleep(0.35)` beserta pemanggilan `_advance_to_next()` agar tidak tertumpuk secara asinkron (*overlap*). Segala rentetan kejadian *EOF* instan yang hadir sementara *flag* sedang diset ke `True` akan ditolak seketika (BUG-B18, EXC-04).
- **Files Modified**: 
  - `engine/playback/controller.py`
  - `tests/unit/engine/test_playback_controller_eof_parallel.py` (New Test)
- **Validation Result**: Lulus. Pengujian `asyncio.gather` dengan melontarkan 2 _event EOF_ ganda pada iterasi *event loop* yang sama tervalidasi hanya memicu `_advance_to_next` sebanyak eksak 1 (satu) kali.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
