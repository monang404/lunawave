# Task ID: S02-001

**Title:** AppState adalah mutable shared state global
**Priority:** P1
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[EXEC-001, ARCH-A07, MAINT-C-01, CS-016, CS-002]

## Root Cause
Objek AppState yang termutasi via WebSocket dijalankan dari banyak thread coroutine, namun eksekusi modifikasi data list-nya (queue, lirik) tidak sepenuhnya terkunci dalam _lock. Python GIL tak cukup untuk proteksi tingkat asinkron dan ini memicu kemungkinan korupsi daftar. AppState menyatukan player state, UI state, download state, dan network state menjadi satu struct yang tidak modular. Kelas lain harus memuat keseluruhan state hanya untuk membaca sebagian state spesifik. AppState digunakan s...

## Location
`server/handlers/ws/discover_handlers.py:81-86`

## Current Behavior
`AppState` yang didefinisikan di `core/state.py` merupakan sekadar dataclass, sementara kode handler seperti contoh di atas langsung memodifikasi atribut (`state.current_track.is_favorite`) dari coroutine berbeda secara _bypassing_ tanpa kunci (lock) maupun event bus.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Gunakan mekanisme penguncian (contoh: asyncio.Lock) untuk mencegah race condition. Bungkus akses variabel mutasi dalam blok `async with lock:`.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
['M-122']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-001
- **Objective**: Mencegah *race condition* saat memutasi data `AppState` secara asinkron.
- **Summary**: Menambahkan atribut bawaan `lock: asyncio.Lock` pada inisialisasi dataclass `AppState` di `core/state.py`. Kemudian di `server/handlers/ws/discover_handlers.py`, kode yang bertugas menyinkronkan status *favorite* (`is_favorite`) kini dibungkus secara absolut menggunakan *context manager* `async with state.lock:` sehingga mutasi bersama dari ragam WebSocket _coroutine_ berjalan secara _thread-safe_.
- **Files Modified**: 
  - `core/state.py`
  - `server/handlers/ws/discover_handlers.py`
  - `tests/unit/core/test_state_lock.py` (New Test)
- **Validation Result**: Lulus. Unit test memverifikasi pembentukan instansi kunci `asyncio.Lock` saat inisiasi kelas serta kepemilikannya.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
