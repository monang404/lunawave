# Task ID: S02-013

**Title:** on_next memicu bottleneck beruntun karena hold _lock
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B08]

## Root Cause
on_next memegang lock sambil mengeksekusi _advance_to_next() -> play_track(). Hal ini menyebabkab semua operasi I/O dan jaringan panjang terperangkap di dalam satu lock secara sekuensial, menghentikan seluruh antrean operasi lain.

## Location
`engine/playback/playback_commands.py:29-35`

## Current Behavior
Handler `on_next` menahan lock terpusat `_lock` di saat yang sama memanggil `_advance_to_next` (yang mengurus I/O panjang), mengakibatkan bottleneck parah di seluruh command lainnya.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Gunakan mekanisme penguncian (contoh: asyncio.Lock) untuk mencegah race condition. Bungkus akses variabel mutasi dalam blok `async with lock:`.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
Tidak ada

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-013
- **Objective**: Mencegah *bottleneck* I/O pada antrean _event handling_ akibat penguncian (*lock*) yang terlalu lama saat mengeksekusi fungsi asinkron berdurasi panjang `_advance_to_next` atau `play_track`.
- **Summary**: Fungsi _commands_ `on_next`, `on_prev`, dan `on_play_track` kini melepas (release) `self.playback_controller._lock` sebelum mengeksekusi `_advance_to_next()` atau `play_track()`. _Lock_ hanya memproteksi pemeriksaan/perubahan ringkas objek _state_ dan tidak turut mengurung aktivitas resolusi jaringan yang bersifat _blocking/long-running_. Ini menghapus jebakan sekuensial _deadlock/bottleneck_ pada kontrol pemutaran.
- **Files Modified**: 
  - `engine/playback/playback_commands.py`
  - `tests/unit/engine/test_playback_commands_lock.py` (New Test)
- **Validation Result**: Lulus uji. Unit test memastikan `_lock` tak lagi berstatus diakuisisi (*held*) pada rentang pemanggilan `_advance_to_next()` dan `play_track()`.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
