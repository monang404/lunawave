# Task ID: S02-005

**Title:** Memory leak pada _stream_rate_limit
**Priority:** P1
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[EXEC-013, EXEC-039, PERF-P14, EXC-03]

## Root Cause
Variabel _stream_rate_limit (defaultdict) bertambah tanpa batas karena tidak ada pembersihan (pruning) untuk data usang, menyebabkan memory leak saat traffic tinggi. Perlu ada proses pruning ke dalam _stream_rate_limit agar tidak menyebabkan kebocoran memori (disamakan polanya dengan auth.py). Key IP Address penanda trafik stream dari _stream_rate_limit akan tetap tersimpan meski timestamp-nya sudah dieliminasi, menguras RAM berangsur-angsur apabila pengunjung/konektor berganti IP tanpa batas. V...

## Location
`server/handlers/http.py:17`

## Current Behavior
Variabel `_stream_rate_limit` hanya menambah riwayat (history) pada line 62 tanpa pernah menghapus record/IP yang sudah usang jika user tidak mengakses lagi, memicu memory leak murni.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Implementasikan mekanisme garbage collection atau batas ukuran (max limit) serta timeout/pruning untuk mencegah kebocoran memori (memory leak).

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
['M-036']

## Conflicts With
['M-007', 'M-077', 'M-117', 'M-190']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-005
- **Objective**: Mencegah insiden *memory leak* akibat kegagalan penghapusan _key_ usang pada *dictionary* _rate limiting_.
- **Summary**: Merombak mekanisme _pruning_ riwayat pada fungsi `_enforce_rate_limit` di modul `server/handlers/http.py`. Blok limitasi _hardcode_ (seperti `len > 1000`) dihapuskan dan diganti dengan rutinitas iterasi reguler yang mengecek *stale IPs*. *Entry* alamat IP yang seluruh *timestamp*-nya sudah melewati batas kadaluwarsa (60 detik) akan di-*pop* sepenuhnya keluar dari *defaultdict*, mengadopsi standar pembersihan yang serupa dengan metode pada `auth.py`.
- **Files Modified**: 
  - `server/handlers/http.py`
  - `tests/unit/server/test_http_rate_limit_prune.py` (New Test)
- **Validation Result**: Lulus. Unit test `test_prune_stale_ips` menstimulasikan aliran waktu (menggunakan fungsi simulasi `time.monotonic`) dan memvalidasi lenyapnya entri *key* _dictionary_ yang tidak lagi _fresh_.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
