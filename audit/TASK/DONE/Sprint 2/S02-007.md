# Task ID: S02-007

**Title:** discover_service KeyError: stream_url tidak di-SELECT
**Priority:** P2
**Sprint:** sprint-02-stability
**Status:** DONE

## Source Audit
[BUG-B01, ARCH-A01, BUG-01]

## Root Cause
DiscoverService mengambil data dari database namun melupakan field stream_url dari query SELECT. Imbasnya, ketika sistem mencoba membuat instansi TrackInfo, terjadi KeyError yang membuat seluruh tab Discover crash dan tak dapat berfungsi. Kueri SQL tidak meminta kolom stream_url pada pemilihan data track, namun kode mengasumsikan keberadaannya pada variabel dictionary yang dilempar, memicu KeyError fatal yang mencrash fungsi tab Discover secara keseluruhan. SQL pada fungsi DiscoverService.get_re...

## Location
`server/services/discover_service.py:36`

## Current Behavior
Kode pada blok kueri SQLite sama sekali tidak men-SELECT kolom `stream_url`, namun pada inisialisasi TrackInfo, kunci map dict `d["stream_url"]` diakses secara mutlak yang memicu KeyError fatal.

## Expected Fix
Lakukan refactor/perbaikan pada lokasi kode terkait agar sesuai dengan arsitektur yang aman dan benar. Jangan menggunakan blok 'bare except' atau Exception generik. Tangkap spesifik Error yang diharapkan (misal ValueError/KeyError) dan log dengan formatter yang tepat menggunakan logger sistem, bukan sekadar diprint.

## Constraints
- Tidak boleh merusak kapabilitas streaming inti dari player MPV.
- Modifikasi arsitektur tidak boleh mengorbankan performa saat melayani banyak websocket connection.
- Ikuti standard code quality dan format di `Task_execution_rules.md`.

## Dependencies
Tidak ada

## Conflicts With
['M-064']

## Success Criteria
- [x] Build berhasil
- [x] Lint berhasil
- [x] Test terkait pass (sebutkan nama test jika sudah ada)
- [x] (jika area belum ada test) Tulis minimal 1 test baru yang membuktikan fix bekerja
- [x] Tidak ada regression pada modul terkait

## Completion Report
- **Task ID**: S02-007
- **Objective**: Mengatasi *KeyError* dan memperbaiki manajemen *exception* bare blok pada kelas DiscoverService.
- **Summary**: Menyelipkan pemanggilan _field_ `stream_url` pada skrip sintaks SQL `SELECT` internal modul `server/services/discover_service.py` untuk fungsi ekstraksi _recent, favorites, cached_. Modifikasi pada inisialisasi `TrackInfo` dengan menyematkan metode `d.get("stream_url")` memberi perlindungan *fallback* bila data bernilai null/tidak disertakan. Lebih jauh, blok tangkapan `except Exception` mentah di-_refactor_ agar spesifik membidik turunan eror `(KeyError, ValueError, sqlite3.Error)` lalu menuliskannya di-_log_ (`logger.error`), alih-alih `print` biasa.
- **Files Modified**: 
  - `server/services/discover_service.py`
  - `tests/unit/server/test_discover_service.py` (New Test)
- **Validation Result**: Lulus. Unit test menjamin tiadanya lagi kasus jatuhnya aplikasi (*crash*) akibat KeyError pada momen de-serialisasi data trek temuan.
- **Documentation Updated**: CHANGELOG.md diperbarui.
- **Status**: DONE
